Final Demo Runbook (3:40)

The submitted recording is `demo/final_demo_video.mp4`. It contains synchronized English narration, a normal real-input run, two edge cases, evaluation, and one batch workflow. Captions and the exact narration are stored beside the video.

1. Introduce the advisor decision-support use case and the human-in-the-loop boundary.
2. Explain the route -> retrieve -> classify -> govern -> review architecture.
3. Run `python -m msba_job_matcher.app --input-json demo/sample_query.json --output outputs/final_demo_output.json`.
4. Verify `input_mode=search`, the explicit US constraint, grounded job evidence, and no invented posting label for the search sentence.
5. Run `python -m msba_job_matcher.app --input-json demo/edge_case_query.json --output outputs/final_edge_case_output.json`.
6. Verify that `high_fit` is preserved while explicit `NO OPT/CPT` evidence forces `review_required=true` and a Hold action.
7. Show the thin-metadata case from `python -m msba_job_matcher.casebook`; missing core fields override the displayed label to `unclear`.
8. Report fixed-set, temporal, calibrated-review, and hard-constraint audit results with the weak-label caveat.
9. Run `python -m msba_job_matcher.batch --input demo/batch_review_input.csv --output outputs/batch_review_results.csv` and show preserved reasons and evidence.
10. Close with the advisor queue, authorization benchmark, governance boundary, and recommended supervised pilot.

Audio/visual QA requirements

- Duration between 3:00 and 5:00.
- One H.264 or MPEG-4 video stream and one AAC audio stream.
- Audible narration throughout all ten scenes; no clipping or silent replacement track.
- 1280 x 720 output with legible commands, metrics, and policy results.
- The edge case visibly includes the explicit restriction and resulting Hold action.
